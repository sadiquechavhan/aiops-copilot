exporters:
  kafka:
    brokers: ["localhost:9092"]
service:
  pipelines:
    metrics:
      receivers: [otlp]
      exporters: [kafka]
      processors: [batch]
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
processors:
  batch: {}
extensions:
  health_check: {}
